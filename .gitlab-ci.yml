stages:
  - test
  - deploy

include:
  - template: Security/SAST.gitlab-ci.yml

sast:
  stage: test

pages:
  stage: deploy
  image: node:20
  script:
    - npm ci
    - npm run build
    - mv out public
  artifacts:
    paths:
      - public
  only:
    - main